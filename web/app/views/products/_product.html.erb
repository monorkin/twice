<div id="<%= dom_id(product) %>" class="relative flex flex-row p-2 border border-gray-400 border-t-0 first:border-t first:rounded-t-md last:rounded-b-md">
  <div>
    <%= product.name %>
  </div>
  <%= link_to "", product, class: "absolute inset-0" %>
</div>
