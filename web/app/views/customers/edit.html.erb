<div class="flex flex-col gap-6 w-full">
  <div class="flex flex-row justify-between items-center">
    <h1 class="font-semibold text-2xl">Edit Customer</h1>
    <%= link_to "Back to customer", @customer, class: "p-2 border border-black rounded" %>
  </div>

  <div>
    <%= render "form", customer: @customer %>
  </div>
</div>
